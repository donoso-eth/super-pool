export interface INETWORK_CONFIG {
  [key: string]: {
    ops: string;
    opsExec:string;
    host: string;
    sfResolver:string;
    token: string;
    superToken: string;
    aavePool: string;
    aToken: string;
    aaveToken:string;
  };
}
